# 阶段 1 交付报告

**交付时间**：2026-09-16
**交付物**：`D:\agent\dsh-wps\`（26 个文件，约 147 KB）
**状态**：阶段 1 完成，等你验收

---

## 一、做了什么

把「能填 Key 的实物」做出来了，并且服务端是真能用的。

| 模块 | 内容 |
|---|---|
| **WPS 加载项** | Ribbon 双按钮、加载即自动打开侧栏、首次使用自动弹设置、前端错误上报 |
| **聊天侧栏** | 停靠式、流式输出、可中断、**可引用当前选区**、配置状态实时显示 |
| **配置悬浮框** | 非模态浮窗，9 厂商预设、自动填 baseURL、Key 显示/隐藏/清除、测试连接 |
| **本地服务** | 零第三方依赖、静态托管、SSE 流式转发、错误人话化、客户端日志回传 |
| **安装/卸载** | 一键注册三个组件、自动备份、合并安全、注册开机自启、反向清理 |
| **文档** | README（含排错章节）、AGPL-3.0 全文 |

---

## 二、验证结果

### 服务端接口自测：18 / 18 通过

```
OK  服务存活 /api/ping                       version=0.1.0
OK  厂商预设 /api/providers                  9 个预设
OK  静态托管 加载项入口 /                     HTTP 200
OK  静态资源 ×7（ribbon/manifest/api.js/bootstrap/chat/settings/图片）
OK  目录穿越防护 /../package.json             HTTP 404
OK  保存配置 POST /api/config                hasKey=true
OK  回读配置 且绝不回传 Key 本身               未泄漏
OK  错误翻译：baseURL 不通                    网络请求失败（人话）
OK  未填 Key 时的友好拒绝                     还没填 API Key
OK  真实厂商连通性（假 Key 打 DeepSeek）        API Key 无效或已过期 (147ms)
OK  对话接口 SSE                             含错误事件=true
```

> **最有价值的一条是倒数第二条**：它真的向 `api.deepseek.com` 发了请求、真的收到了 401、
> 并且被正确翻译成了「API Key 无效或已过期」。这是**端到端**验证，不是模拟。

### 页面静态校验：19 / 19 通过

内联脚本语法、元素 id 交叉校验（12 + 14 个引用全部命中）、
脚本引用存在性、加载项必需文件、**ribbon 回调全部已实现**。

### 页面运行时模拟：21 / 21 通过（后补，价值最高）

无头 Edge 被沙箱拦住，所以写了一个极简 DOM 模拟器（`dev/page-sim.mjs`），
**真正执行页面脚本**、对着真实服务发请求，并模拟点击：

```
OK  厂商下拉被填充了 9 项
OK  baseURL 自动填充 = "https://api.deepseek.com/v1"
OK  模型名自动填充 = "deepseek-chat"
OK  状态栏文案 = "已配置"
OK  点「测试连接」后结果区可见（class=result show err）
OK  结果区文案 = "API Key 无效或已过期 重新复制 Key…"
OK  测试按钮已恢复可用（没有卡在 loading）
OK  保存后状态栏 = "已保存"
OK  服务状态点 = "dot ok" / 模型信息栏 = "deepseek-chat · 修订留痕"
OK  发送后消息区更新、输入框清空、按钮恢复
OK  失败横幅 = "API Key 无效或已过期 … HTTP 401"
```

**这一层立刻抓到了一个真 bug**（见第六节第 9 条），静态检查永远发现不了。

### 安装脚本：逻辑已验证

把 `APPDATA` 重定向到工作区实跑了一遍：

- ✅ 生成正确的 `publish.xml`（文字/表格/演示三条，URL 带结尾斜杠）
- ✅ **合并安全**：注入一条模拟的第三方加载项后重跑，第三方条目完好保留
- ✅ 自动备份原文件
- ✅ 卸载只摘掉自己的三个条目，第三方条目不受影响

---

## 三、没能验证的（重要，请知悉）

| 项 | 原因 |
|---|---|
| **加载项在真实 WPS 里加载** | 需要写 `%APPDATA%\kingsoft\wps\jsaddons\`，在当前沙箱下被拒绝；按你的要求我没有发审批弹窗。**这一步只能由你双击 `点我启动助手.cmd` 完成。** |
| 页面在浏览器里渲染 | 无头 Edge 被沙箱拦住（`crashpad OpenProcess: 拒绝访问`），改用静态校验替代 |
| 真实模型对话 | 没有真实 API Key |

**换句话说：后端是真跑通的，前端是静态校验通过但未在 WPS 里跑过。**
如果前端有运行时问题，服务端窗口会打出 `前端报错 [...]`，照着改就行。

---

## 四、你怎么验收

1. 双击 **`点我启动助手.cmd`**（它会注册加载项 + 拉起服务 + 注册开机自启）
2. **完全退出 WPS**（任务管理器里确认 `wps.exe` 都结束了），重新打开
3. 顶部「AI 助手」标签页 → 「打开助手」→ 侧栏会自动弹出模型设置
4. 选厂商、粘贴你自己的 Key、点「测试连接」、保存
5. 选中一段文字，在侧栏里问一句

**如果第 3 步标签页没出现**，看 README 的「排查」章节 —— 九成是 WPS 进程没退干净。

---

## 五、阶段 1 踩到的坑（已写进代码/文档）

1. **关 WPS 窗口 ≠ 重启 WPS**。`publish.xml` 只在进程启动时读，残留进程会导致加载项不出现。已写进安装提示和 README。
2. **加载项无法启动外部进程**。实测 `CoCreateInstance('WScript.Shell')` 报 `can not create`，所以本地服务只能靠安装脚本注册自启。
3. **`WpsInvoke` 在 12.1 上是 undefined**。官方示例里的 `WpsInvoke.CreateXHR()` 会直接报错，本项目改用原生 `XMLHttpRequest`。
4. **WPS 里开不了 DevTools**。所以加了 `/api/clientlog` 通道，前端任何报错都会回传到服务端控制台。
5. **沙箱会拦带管道的命令**。`.ps1` 的语法检查不能用 `2>&1 |`，这是开发环境问题，不影响产品。

---

## 六、真机验证轮又挖出 5 个 bug（全部已修）

用户实际运行 `点我启动助手.cmd` 后"没反应"，顺着查下去发现了这些。**这些在实验室自测里全部测不出来。**

| # | Bug | 后果 | 根因 |
|---|---|---|---|
| 6 | **VBS 启动器的引号被 cmd 吃掉** | **服务从来没起来过** —— 这就是"装完没反应"的直接原因 | `cmd /c "a.cmd" "b.js"` 的首尾引号会被 cmd 的解析规则吞掉，命令静默失效。改成 `.cmd` 包装脚本 + 只负责隐藏窗口的 `.vbs` |
| 7 | **PowerShell 逗号优先级高于加号** | 第一版修完生成的文件**仍然是坏的**：路径被拆成多行 | `@('a', 'b' + $x + 'c')` 会被展平成 4 个元素，`-join` 再插换行。每条拼接必须用括号包住 |
| 8 | **`.ps1` 缺少 UTF-8 BOM** | 脚本直接语法崩，报一堆莫名的"意外的标记 }" | Windows PowerShell 5.1 把无 BOM 的 UTF-8 当 GBK 读，**中文全角字符的尾字节和后面的 ASCII 引号被配成一个双字节汉字，把引号吃掉** → 字符串失去闭合 → 全线崩 |
| 9 | **「留空 = 不修改」实际会删掉 Key** | 用户只是改个模型名，Key 就被清空了 | 界面文案承诺"留空不修改"，但代码把空串当"清除"。改成区分「没填/空串」与显式 `clearKey` |
| 10 | **自启注册失败会中断整个安装** | 组策略或安全软件拦注册表时，后面所有步骤都不执行 | 加 try/catch 降级：提示"需要手动启动"，继续往下走 |

### Bug 8 的副产品

差点被误判：我曾把文件开头的字节 `35,82,101` 当成了 BOM，实际那是 ASCII 的 `#Re`（`#Requires`）。
白绕了一圈才确认 PowerShell 5.1 读中文脚本**必须**有 BOM。

### 其他

- 给服务加了 `DSH_WPS_LOG` 文件日志（隐藏窗口启动时唯一的排查手段）
- 静态请求日志加了 `[WPS]` / `[非WPS]` UA 标记，用来区分"加载项真的在 WPS 里跑了"和"测试脚本访问了"
- 自测从 37 项增加到 **59 项**（20 静态 + 21 页面运行时 + 18 接口）

---

## 六、真机跑通（第二轮验证，成功）

修完第五节和第六节的 bug 后，在真实 WPS 12.1 上完整跑通。
服务端日志（`[WPS]` 标记 = 来自 WPS 内嵌浏览器）：

```
[22:02:41] 静态 200: [WPS] /index.html / main.js / js/api.js / js/bootstrap.js
[22:02:42] 追踪 [addin-main] [trace] boot 开始 typeof_Application=object
[22:02:42] 追踪 [addin-main] [trace] ping 完成 serviceOk=true
[22:02:42] 追踪 [addin-main] [trace] CreateTaskPane 调用中…
[22:02:42] 追踪 [addin-main] [trace] 侧栏创建成功 ID=1
[22:02:42] 静态 200: [WPS] /ui/chat.html
[22:02:42] 追踪 [addin-main] [trace] 配置读取完成 hasKey=true
[22:03:03] 对话结束，用时 157ms
[22:03:06] 静态 200: [WPS] /ui/settings.html
```

截图确认的界面状态：

- ✅ Ribbon 上出现「AI 助手」标签页
- ✅ 右侧聊天侧栏自动打开
- ✅ 配置悬浮窗正常弹出：9 个厂商预设已填充、baseURL 和模型名自动填好、
  「留空 = 不修改」说明到位、写入方式默认选中「修订留痕」
- ✅ 在侧栏里发出消息，消息气泡正常渲染
- ✅ 用假 Key 触发 401，界面上显示了人话错误横幅
- ✅ **零真实前端报错**（日志里的"追踪"是打点，不是错误）

### 卡住这轮的元凶（第 11 个 bug）

**一个控件同时写 `label` 和 `getLabel`，WPS 会静默拒绝整份 `ribbon.xml`。**

表现：没有报错、没有日志、标签页不出现、**`onLoad` 也不触发** ——
看起来像整个加载项死掉了，实际连入口都没进去。
这是 Office Fluent UI 的规则，WPS 官方模板里从不混用。

修法（三处，不只修表面）：

1. 去掉冲突属性
2. **加了一条不依赖 Ribbon 的启动路径** —— 脚本加载即计时启动，
   `booted` 标志保证两条路只生效一次。以后 Ribbon 再出问题，侧栏照样能开
3. 把静默吞掉的 `try/catch` 改成回传错误，并在每个阶段打点（`dshTrace`）

并且把这个坑做成了**自动检查**（`check-pages.mjs` 里新增的一项），以后谁再写成对属性都会被测出来。

---

## 七、下一步（阶段 2 / 3）

### 问得最多的一个问题：为什么现在还不能直接改文档？

**不是做不到 —— 阶段 0 已经把全部前置能力实测验证过了。**

| 能力 | 实测结果 |
|---|---|
| 侧栏里能拿到 `window.Application` | ✅ `canReachDocument: true`，能读到当前文档名 |
| 用代码撤销程序写入的内容 | ✅ `Document.Undo()` 真实生效 |
| 把**一轮**改动打包成一次 Ctrl+Z | ✅ `Application.UndoRecord` 带 `StartCustomRecord` / `EndCustomRecord` |
| 改动以修订形式出现、逐条接受/拒绝 | ✅ `TrackRevisions` + `Revisions.Accept()` / `Reject()` |

**所以缺的不是能力，是三样还没写的东西：**

**① 工具层。** 没有任何「读选区 / 替换文字 / 插入内容」的工具函数。

**② 会用工具的后端。** 阶段 1 的本地服务是**直连厂商**的最小实现 ——
把消息转发给模型、把流式文本拿回来。模型只能"说话"，没有"手"。

```
现在   侧栏 → 本地服务 → 厂商 API → 流式文本 → 侧栏显示
       （一问一答，模型碰不到文档）

要做   侧栏 → 本地服务（agent 循环）→ 模型说「调用 replace_text(旧, 新)」
                  ↓                              ↑
           反向调用侧栏 → 侧栏用 JSAPI 改文档 → 结果回给模型 → 继续
```

**③ 反向通道。** 文档操作**只能在加载项上下文里执行**（只有它持有 `window.Application`），
但 agent 循环跑在本地服务里。所以需要「服务 → 侧栏 → 文档」的反向调用。
现在的 `DSH.chat` 是单向的（服务 → 侧栏 SSE）。WebSocket 是天然答案，
而且**阶段 0 已实测加载项能建 WebSocket**。

**为什么不在阶段 1 顺手做掉：** 写入是有风险的，必须先有安全兜底 ——
默认走修订留痕（改动以修订出现、逐条确认），一轮改动打包成一次撤销。
安全兜底不做就去动用户文档，是个坑。这三件事（工具层 / agent 循环 / 写入策略）
是一体的，放在阶段 3 一起做才稳。

**现在的实际能力边界：** 选中文字 → 提问 → 模型**基于选区**回答 →
**结果需要用户自己复制粘贴回文档**。

---

**阶段 2 —— 接入 DSH 运行时**：把现在这个"直连厂商"的临时后端换成 DSH，
获得多轮会话、工具调用、会话持久化、子 agent。**这也是阶段 3 的前置**——
agent 循环由 DSH 提供，我们只需要把文档操作注册成它的工具。

**阶段 3 —— 文档能力**（前置能力已全部验证）：

- 读选区 / 读全文（长文档需要分段策略，不能整篇塞上下文）
- 把文档操作注册成工具：替换文字 / 插入内容 / 加批注
- **默认走修订留痕**：改动以修订形式出现，用户逐条接受或拒绝
- **整轮撤销**：用 `Application.UndoRecord.StartCustomRecord` / `EndCustomRecord`
  把 agent 一轮的所有改动打包成一次 `Ctrl+Z`
- 安全兜底：写入前记录原文，失败可回滚

**阶段 4 —— 分发**：打包 Node 运行时（现在要求用户自己装是硬伤）、单文件安装包。

---

## 八、仍然开放的产品问题

这几条还没定，会直接影响阶段 3：

1. **权限边界**：助手除了当前文档，能不能读本地文件、联网、执行命令？
   （DSH 有审批和沙箱机制，但 WPS 侧栏里怎么呈现还没设计）
2. **首次配置之外的默认值**：默认模型、默认温度、默认写入方式
3. **合规**：文档内容发往模型 API 的用户告知怎么做
