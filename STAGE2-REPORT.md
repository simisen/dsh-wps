# 阶段 2 交付报告：助手能直接改文档了

**交付时间**：2026-09-16
**状态**：功能完成，自动化验证通过；**真实 WPS 里的 JSAPI 行为待你在真机确认**

---

## 一句话

选中一段文字说「帮我润色」，助手**直接改在文档里**（走修订留痕）；
不满意按一次 `Ctrl+Z` 整轮退回。

---

## 做了什么

| 模块 | 内容 |
|---|---|
| **文档工具层** | 6 个工具：读文档信息 / 读选区 / 通读正文 / 替换选区 / 插入 / 全文替换。全部走 WPS JSAPI |
| **agent 循环** | 模型 ↔ 工具多步循环，最多 6 步，超时与断线都有兜底 |
| **协议适配** | 带 function calling 的**流式**适配，OpenAI 兼容 + Anthropic 两套（含 tool_calls 增量累积） |
| **反向通道** | SSE 下行推工具请求 + `POST /api/tool-result` 上行回结果 |
| **安全兜底** | 修订留痕（默认）+ 整轮撤销打包，只在**真正写文档时**才开启 |
| **运行时接缝** | agent 循环做成可替换接口，接 DSH 时只换实现 |

---

## 验证：72 项自动化检查全绿

| 测试 | 项数 | 结果 |
|---|---|---|
| 页面静态校验 | 21 | ✅ |
| 接口自测 | 18 | ✅ |
| 页面运行时模拟 | 21 | ✅ |
| **工具调用链端到端** | **12** | ✅ |

### 端到端测试的实际输出（这是重点）

```
改前  : 这是一段原始的示例文字。
改后  : 这是一段改写过的示例文字。

撤销记录: StartCustomRecord×1  EndCustomRecord×1
修订开关切换次数: 2

文档操作: [
  {"op":"undo_start","name":"DSH 助手修改"},
  {"op":"replace_selection","from":"原始的示例文字","to":"改写过的示例文字"},
  {"op":"undo_end"}
]
```

**断言的是文档对象真的被改了、撤销记录真的开了又关**，不是"函数被调用了"。

### 多步循环也验证了

模拟模型日志（`get_selection` → `replace_selection` → 收尾）：

```
请求：2 条消息，已收到 0 个工具结果，提供工具 6 个  → 第 1 步：调用 get_selection
请求：4 条消息，已收到 1 个工具结果，提供工具 6 个  → 第 2 步：调用 replace_selection
请求：6 条消息，已收到 2 个工具结果，提供工具 6 个  → 返回文本
```

每轮都把上一轮的工具结果带回去了，循环正常收敛。

### 怎么做到的

新增了两个测试设施：

- `dev/mock-llm.mjs` —— **模拟模型服务**，脚本化返回 `tool_calls`。不需要真 Key、不花钱
- `dev/sim-env.mjs` —— 模拟的 WPS `Application`（可断言的假文档对象）

两者合起来，整条链路就能在没有真实模型、没有真实 WPS 的情况下端到端跑通。

**这套设施立刻抓到了 3 个测试夹具自身的 bug**（classList 与 className 不同步、
querySelectorAll 空实现、取文本时漏读 innerHTML）—— 这些会让测试结果不可信，
修完之后断言才有意义。

---

## 一个架构决策：运行时做成可替换的

原计划阶段 2 是「接入 DSH 运行时」。动手时发现两条硬约束：

1. **DSH 唯一公开的对外接口是 stdio JSON-RPC**
   （`dsh-sdk-protocol` 的包描述写得很明确；`dsh-api-gateway` 是它自己 GUI 的私有通道）
2. **开发环境禁止创建子进程管道**（实测 `piped spawn → EPERM`）

两条加起来意味着：DSH SDK 这条路**在本环境无法自测**，而且 DSH 的 SDK 文档
在打包版里被剥掉了，协议得从编译产物反推。

所以改成：**先把 agent 循环做在进程内，但放在可替换的接口后面**。

```
server/agent/loop.mjs   ← 运行时接缝
   ├─ createInProcRuntime()   现在：进程内循环（原生 function calling）
   └─ （以后）createDshRuntime()   接 DSH，上层完全不用改
```

好处是：文档能力先落地并且**可验证**；接 DSH 时是一次替换，不是重写。

---

## 还没做 / 还没验的

### ✅ 真机确认（2026-09-16，用户实测）

上面列的三处不确定性**已在真实 WPS 12.1 上验证通过，无报错**：

| 项 | 结论 |
|---|---|
| `sel.Range.Text = 新文字` 替换选区 | ✅ 可用 |
| `UndoRecord.StartCustomRecord` / `EndCustomRecord` | ✅ 可用，整轮撤销成立 |
| 修订留痕开关 + 复原 | ✅ 可用 |
| **`insert_text`（光标处插入）** | ✅ 可用 |
| **`replace_all`（`Content.Find.Execute()`）** | ✅ **可用** —— 这是当初标"最没把握"的一处，WPS 的 Find 对象模型跟 Word 一致 |

**结论：文档能力在真机上全部跑通了**，不再只是模拟环境里的验证。

`replace_all` 的验证证据（不只"返回 ok"，而是**文档正文真的变了**）：

```
文档正文：  这是多进程打（替换成功）开的测试文档
                              ↑ 红色修订标记
服务日志：  工具结果 [insert_text]  ok=true     00:32:02
            工具结果 [replace_all]  ok=true     00:32:29
            对话结束：2 步，工具[replace_all] 用时 1696ms
```

模型也选对了工具：说"插入"用 `insert_text`，说"全文…替换"用 `replace_all`。

### 用户反馈的三个问题（已全部修复并验证）

| # | 现象 | 根因 | 修法 | 验证方式 |
|---|---|---|---|---|
| 1 | 点一次「设置」弹出**两个**相同窗口 | 侧栏的 `openSettings()` 有两条路：广播给加载项主上下文 + "兜底"自己也调 `ShowDialog`。我以为兜底不会触发，但**侧栏确实也持有 `window.Application`**，两条都成功 | 广播成功即返回；只有广播通道不可用时才走兜底。加载项侧再加 800ms 防抖 | 真机日志：`settings.html` 请求次数 **2 → 1** ✅ |
| 2 | 输出不了长文本 | ① `maxTokens` 默认写死 2048（约 1500 汉字），且界面上没暴露；② 系统提示词写了「回答要简洁」「不要长篇大论」，模型一律往短答 | ① 默认提到 8192 + 设置里加「高级 → 最大输出长度」；② 重写篇幅约束，区分"回答"和"内容"；③ 被截断时**明确提示**（文本截断提示语，工具参数截断直接拦截） | 模拟模型自省：服务把 `max_tokens=8192` 传给了厂商 ✅ |
| 3 | **每次都要重新输入 API Key** | 我图测试方便用 `DSH_WPS_HOME` 指到临时目录，Key 存那边；用户自己启动服务读的是 `%APPDATA%\dsh-wps`，那里什么都没有 —— 两个目录分叉。而且 Key 输入框永远是空的（安全设计），看不出"其实已保存" | ① 把凭据和配置迁移到正式目录；② 输入框显示 `••••••••（已保存，留空即不修改）`；③ 加回归测试「留空保存不会弄丢 Key」 | 正式目录实例自报 `API Key 已设置`；真机启动无"还没配置模型"横幅 ✅ |

### 🔒 顺带堵掉一个凭据泄漏风险

排查问题 3 时发现：**用户的真实 Key 在项目目录里还有一份副本**
（`dev/testhome/credentials.json`）。这个项目是要开源分享的 —— 打包发出去 Key 就跟着泄漏了。

- 删除项目内所有凭据文件，确认干净
- 新增 `.gitignore` 屏蔽 `credentials.json` / `*.key` / `.env` / `dev/testhome*`
- README 加了醒目提醒

### 测试隔离（教训）

上面问题 3 暴露了一个**测试设计失误**：自动化测试原本跑在用户的实例上，
而测试会写假 Key、还会真的发模型请求 —— 等于**覆盖用户的 Key + 花用户的钱**。

现在：
- 测试默认打 **43131 专用实例**，不再碰 43130（用户的）
- 测试**无条件**把测试实例的 Key 置为假 Key
- 隔离步骤写进 README，跑测试前必须先起独立实例

### 其他还没做的

- 表格 / 演示组件的文档工具（现在只适配了文字）
- 长文档的分段策略（现在正文最多读 20000 字）
- 接 DSH 运行时（见下）
- 侧栏里「允许修改文档」的总开关（现在靠修订留痕兜底）

---

## 建议你这样验

在真实 WPS 里依次试：

1. 选中一段文字 → 输入「帮我润色一下」→ **看是否出现修订标记**
2. 按一次 `Ctrl+Z` → **看是否整轮退回**（而不是只退一个字）
3. 不选中任何文字 → 输入「帮我改改」→ **看它是否会先问你要改哪段**（不该擅自改全文）
4. 输入「把全文的"用户"改成"客户"」→ 看批量替换是否工作

任何一步不对，把服务端日志（`%APPDATA%\dsh-wps\service.log`）里的
`工具执行失败` 或 `工具结果` 那几行发我，就能定位。
